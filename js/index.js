/**
 * Main entry point for the Rock Paper Scissors game application.
 * Initializes both App and Game classes to handle UI and game logic.
 * 
 * @module index
 */

import { App } from "./app.js";
import { Game } from "./game.js";

// Initialize application and game
const app = new App();
const game = new Game();
